/**
 * MRS / WCH SPL compatibility stub.
 * When importing into MounRiver Studio, replace/enable the full peripheral
 * includes from the CH32V20x SPL pack. This project builds with Platform/
 * register-level HAL and does not require SPL sources for the default Makefile.
 */
#ifndef CH32V20X_CONF_H
#define CH32V20X_CONF_H

/* Intentionally empty for bare Platform HAL builds. */

#endif
